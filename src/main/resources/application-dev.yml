spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orbitrack
    username: orbitrack_app
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration